{
  "name": "react",
  "version": "1.0.0",
  "description": "The `xlsx.core.min.js` and `xlsx.full.min.js` scripts are designed to be dropped into web pages with script tags:",
  "main": "nexthdr.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "react-scripts build",
    "start": "react-scripts start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react-scripts": "^3.0.1",
    "xlsx": "^0.14.4"
  },
  "devDependencies": {}
}
